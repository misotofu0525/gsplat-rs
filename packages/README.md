# Packages

Language-level package wrappers that are not Rust workspace crates.

- `web/`: local `@gsplat-rs/web` browser ESM wrapper around generated
  wasm-bindgen output from `crates/gsplat-web`

Published package distribution is outside the current v0.1 release boundary.
